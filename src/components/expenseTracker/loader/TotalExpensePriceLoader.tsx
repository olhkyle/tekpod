import { SkeletonLoader } from '../../common';

const TotalExpensePriceLoader = () => {
	return <SkeletonLoader width={'270px'} height={'48px'} theme={'dark'} />;
};

export default TotalExpensePriceLoader;
