const queryKey = {
	AUTH: ['auth'],
	PAGE_INFO: ['pageInfo'],
	DIARY: ['diary'],
	DIARY_BY_PAGE: ['diaryByPage'],
	FILM_RECIPE: ['film_recipes'],
	TODOS: ['todos'],
} as const;

export default queryKey;
